import { Sequelize } from "sequelize";
const config = useRuntimeConfig();
export const useSequelize = async () => {
    "ticketvi_back2"
}

export const sequelize = new Sequelize("ticketvi_back2", 'ticketvi_back2', "pHr%DA{!EIhE", {
    dialect: 'mysql',
    host: '23.88.69.107',
    define: {
        timestamps: false,
    },

}) 